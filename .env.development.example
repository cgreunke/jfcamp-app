# ============== DEV ENV ==============

# --- DB ---
DRUPAL_DB_HOST=postgres
DRUPAL_DB_PORT=5432
DRUPAL_DB_NAME=drupal
DRUPAL_DB_USER=drupal
DRUPAL_DB_PASS=drupal

# --- Site / Admin (nur initial) ---
DRUPAL_SITE_NAME=JF Startercamp - App (Dev)
DRUPAL_ADMIN_USER=admin
DRUPAL_ADMIN_PASS=admin
DRUPAL_ADMIN_MAIL=admin@example.com

# --- Config Mgmt ---
# WICHTIG: Pfad muss mit dem im Container verwendeten Pfad übereinstimmen
CONFIG_SYNC_DIRECTORY=/opt/drupal/config/sync
# In DEV beim Start automatisch drush cim -y
DRUPAL_AUTO_IMPORT_ON_START=1

# --- Host & Proxy ---
# Hinweis: DRUPAL_BASE_URL wird von Drupal-Core nicht direkt genutzt; ok, falls von dir/Frontend referenziert
DRUPAL_BASE_URL=http://drupal
DRUPAL_TRUSTED_HOSTS=^drupal$,^localhost$,^127\.0\.0\.1$
DRUPAL_REVERSE_PROXY=0
# Für lokales Reverse Proxy Testing kannst du aktivieren:
# DRUPAL_REVERSE_PROXY=1
# DRUPAL_REVERSE_PROXY_ADDRESSES=172.18.0.10
# DRUPAL_REVERSE_PROXY_TRUSTED_HEADERS=X_FORWARDED_FOR,X_FORWARDED_HOST,X_FORWARDED_PORT,X_FORWARDED_PROTO

# --- Optional: Hash Salt (ansonsten generiert start-drupal.sh einen persistenten) ---
# DRUPAL_HASH_SALT=put-a-long-random-hex-here

# --- API User für Matching ---
DRUPAL_API_USER=apiuser
DRUPAL_API_PASS=apipassword
DRUPAL_API_ROLE=api_writer
DRUPAL_API_MAIL=apiuser@example.com

# --- Matching-Service (Drupal-Seite) ---
MATCHING_BASE_URL=http://matching:5001

# --- Matching-Container ENV (nutzt dein matching_server.py) ---
DRUPAL_URL=http://drupal/jsonapi
DRUPAL_USER=apiuser
DRUPAL_PASS=apipassword
MATCHING_SEED=2027
HTTP_TIMEOUT=10
PAGE_CHUNK=100
MAX_PAGES=1000
PUBLISHED_ONLY=1
LOG_LEVEL=INFO

# --- Sonstiges Build/Install ---
# DRUPAL_INSTALL=auto   # auto|skip (dein Slim-Startskript prüft ohnehin und installiert nur wenn nötig)
DRUPAL_INSTALL_PROFILE=minimal
